#!/bin/sh
# load xresources (if available) before starting dwm (in xinitrc)

if [ -f ~/.config/xresources ]; then
	xrdb ~/.config/xresources
fi
[ -x $(command -v dwmblocks) ] && (dwmblocks &)
exec dwm
